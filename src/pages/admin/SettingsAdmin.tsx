const SettingsAdmin = () => {
  return (
    <div style={{ backgroundColor: 'white', borderRadius: '16px', padding: '40px', textAlign: 'center', color: '#888' }}>
      <p>Settings page — nothing configured here yet.</p>
    </div>
  );
};

export default SettingsAdmin;